# Local Compose values only. Copy this file to .env and create the referenced files.
FRONTEND_PORT=8080
API_DEBUG_PORT=5179
POSTGRES_PASSWORD_FILE=./.secrets/postgres_password
SESSION_SIGNING_KEY_FILE=./.secrets/session_signing_key
# Production values belong exclusively in Neon, Render, Netlify and Resend dashboards.
# Never put provider connection strings, NASA/Resend API keys or proxy signing keys here.
